@inherits LayoutComponentBase
@inject StudyTime.DesktopClient.Services.IPlatformDetector PlatformDetector

@if (PlatformDetector.IsMobile)
{
    <StudyTime.DesktopClient.Components.Layout.MobileLayout>
        @Body
    </StudyTime.DesktopClient.Components.Layout.MobileLayout>
}
else
{
    <StudyTime.DesktopClient.Components.Layout.DesktopLayout>
        @Body
    </StudyTime.DesktopClient.Components.Layout.DesktopLayout>
}

@code {
    // Auth yönlendirmesi Routes/AuthorizeRouteView + RedirectToLogin üzerinden yapılıyor.
    // Burada AuthState await etmek ilk render'ı bloke edip siyah ekrana neden olabiliyor.
}